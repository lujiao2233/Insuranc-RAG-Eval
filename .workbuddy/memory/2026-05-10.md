# 2026-05-10 工作日志

## 完成：系统配置新增"并发生成数"参数

- 需求：将硬编码的 `_concurrency = 3` / `_per_doc_concurrency = 3` 改为可配置
- 后端修改：
  - `backend/services/config_service.py`：DEFAULT_CONFIGS 新增 `generation.concurrency`（默认3），`get_system_config` 新增 generation 分组
  - `backend/services/advanced_testset_generator.py`：
    - `_get_api_key_and_config` 返回值从5元组改为6元组（新增 concurrency）
    - `_generate_with_qwen_llm` 和 `_generate_per_doc_mode` 从 `params.get("_concurrency")` 读取，替代硬编码
    - 所有调用 `_get_api_key_and_config` 的位置同步更新解包
  - `backend/api/routers/testsets.py`：`_run_generation_task` 从 ConfigService 读取 `generation.concurrency`，传入 `params["_concurrency"]`
- 前端修改：
  - `frontend/src/views/config/ConfigView.vue`：
    - `systemConfig` reactive 新增 `generation_concurrency` 字段
    - 模板新增 `el-input-number`（min=1, max=10）
    - `fetchConfigs` 加载 `systemRes.configs.generation['generation.concurrency']`
    - `saveSystemConfig` 保存 `'generation.concurrency'`
- 并发数范围限制：1-10（`max(1, min(10, ...))`）
